run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - revive
    - gocritic

linters-settings:
  goimports:
    local-prefixes: github.com/isklv/pgmsgq

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0